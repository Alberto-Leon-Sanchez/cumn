<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/saladvegan"
        android:label="@string/app_name"
        android:roundIcon="@drawable/saladvegan"
        android:supportsRtl="true"
        android:theme="@style/Theme.CUMN"
        tools:targetApi="31">
        <activity
            android:name=".EmailAuthenticationActivity"
            android:exported="false" />
        <activity
            android:name=".FavouriteRecipes"
            android:exported="false" />
        <activity
            android:name=".LogoActivity"
            android:exported="true"
            android:theme="@style/Theme.CUMN.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RecipeActivity"
            android:exported="false"
            android:theme="@style/Theme.CUMN.NoActionBar" />
        <activity
            android:name=".SignInSignUpActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CUMN.NoActionBar">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>

</manifest>